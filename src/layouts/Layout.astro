
---
import '../styles/global.css';
const base = import.meta.env.BASE_URL || '/';
---

<!doctype html>
<html lang="en" class="scroll-smooth">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Liv Cafe | Oak Bay</title>
	</head>
	<body class="bg-stone-50 text-stone-900 font-sans min-h-screen flex flex-col">
		
		<!-- Navigation -->
		<nav class="bg-white shadow-sm p-4">
			<div class="max-w-4xl mx-auto flex justify-between items-center flex-wrap gap-4">
				<a href={base} class="text-2xl font-bold tracking-tighter text-stone-800">Liv Cafe</a>
				<ul class="flex gap-4 font-medium text-stone-600 flex-wrap">
					<li><a href={`${base}about`} class="hover:text-emerald-600 transition-colors">About</a></li>
					<li><a href={`${base}hours`} class="hover:text-emerald-600 transition-colors">Hours</a></li>
					<li><a href={`${base}menu`} class="hover:text-emerald-600 transition-colors">Menu</a></li>
					<li><a href={`${base}contact`} class="hover:text-emerald-600 transition-colors">Reservations</a></li>
					<li><a href={`${base}catering`} class="hover:text-emerald-600 transition-colors">Catering</a></li>
					<li><a href={`${base}food-to-go`} class="hover:text-emerald-600 transition-colors">Food To Go</a></li>
				</ul>
			</div>
		</nav>

		<!-- Main Content is injected here -->
		<main class="flex-grow">
			<slot />
		</main>

		<!-- Footer -->
		<footer class="bg-stone-900 text-stone-300 py-8 text-center mt-12">
			<p>&copy; 2026 Liv Cafe. Serving the community.</p>
		</footer>

	</body>
</html>